echo "🔍 Lint + typecheck on staged files"
npx lint-staged
LINT_EXIT_CODE=$?

if [ $LINT_EXIT_CODE -ne 0 ]; then
  echo ""
  echo "❌ Lint/typecheck failed!"
  echo "💡 Fix the errors above before committing"
  exit 1
fi

echo ""
echo "✅ Lint checks passed! Committing..."
echo "💡 Tests will run on push"
